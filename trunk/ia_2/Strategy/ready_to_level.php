<?php

function	ready_to_level($player)
{
  /*
    if ($player["time_ok"] != -1 &&
      $player["timer"] - $player["time_ok"] < DELAY_APPEAL)
    return ;
  else
    $player["time_ok"] = -1;


  if ($player["blevel"] == false)
    {
      unset($player["tpidlevel"]);
      $player["tpidlevel"] = array();
      $msg = create_msg(&$player, LEVEL_CENSUS . " " .
			$player["tlevel"][$player["level"]]["joueur"]);
      array_push($player['action'], $msg);
      $player["time_appeal"] = $player["timer"];
      $player["blevel"] = true;
      echo "READY TO LEVEL " . $player['pid'] . "\n";
    }


  if ($player["time_appeal"] != -1 &&
      $player["timer"] - $player["time_appeal"] > DELAY_APPEAL)
    {
      $player["blevel"] = false;
      $player["time_appeal"] = -1;
      echo "FAILED\n";
    }
  */
}

?>